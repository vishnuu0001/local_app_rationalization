# Local development only — loaded by npm start, never by npm run build
# Points to the local Flask backend running on port 5000
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_ENVIRONMENT=development
